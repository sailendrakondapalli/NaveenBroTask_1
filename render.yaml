# Render Static Site Configuration
# This file configures automatic deployment of the Expo web build

services:
  - type: web
    name: powerstackhub-static
    env: static_site
    buildCommand: cd PowerStackHub && npm ci && npx expo export --platform web
    staticPublishPath: PowerStackHub/dist